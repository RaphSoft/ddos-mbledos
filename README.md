Cara menggunakan DDOS mbledos:
1. Isi perintah dibawah ini
   $ apt update && apt upgrade
   $ pkg install python2 && pkg install python
   $ pkg install git
   $ git clone https://github.com/RaphSoft/ddos-mbledos.git
   $ cd ddos-ddos
   $ python2 ddos.py
